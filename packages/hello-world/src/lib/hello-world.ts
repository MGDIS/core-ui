/**
 * this function says hello to the world
 * @param {string} name subject's name
 * */
const helloWorld = (name: string) => {
	// we concat the name to hello
	return `hello ${name}`;
};

export { helloWorld };
