/**
 * this function says goodbye to the world
 * @param {string} name subject's name
 * */
const goodbyeWorld = (name: string) => {
	// we concat the name to goodbye
	return `goodbye ${name}`;
};

export { goodbyeWorld };
