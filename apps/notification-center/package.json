{
  "name": "notification-center",
  "private": true,
  "version": "1.1.1",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3210",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "pnpm lint:es && pnpm lint:prettier",
    "lint:fix": "pnpm lint:es:fix && pnpm lint:prettier:fix",
    "lint:es": "eslint '**/*.ts' --ignore-path ../../.gitignore",
    "lint:es:fix": "eslint '**/*.ts' --fix --ignore-path ../../.gitignore",
    "lint:prettier": "prettier --check '**/*.{html,json}' --ignore-path ../../.gitignore",
    "lint:prettier:fix": "prettier --write '**/*.{html,json}' --ignore-path ../../.gitignore"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "vite": "5.4.10"
  },
  "dependencies": {
    "@mgdis/mg-components": "workspace:*",
    "@mgdis/notification-center": "workspace:*",
    "@mgdis/styles": "workspace:*"
  }
}
