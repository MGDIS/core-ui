import React from 'react';
import DebugConfig from '@theme-original/DebugConfig';

export default function DebugConfigWrapper(props) {
  return (
    <>
      <DebugConfig {...props} />
    </>
  );
}
