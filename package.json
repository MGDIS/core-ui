{
  "name": "core-ui",
  "license": "BSD-3-Clause",
  "packageManager": "pnpm@7.33.6",
  "engineStrict": true,
  "engines": {
    "pnpm": ">=7",
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "gitlab@gitlab.mgdis.fr:core/core-ui/core-ui.git"
  },
  "maintainers": [
    "Simon Duhem <duhem-s@mgdis.fr>",
    "Nicolas Jouanno <jouanno-n@mgdis.fr>"
  ],
  "scripts": {
    "postinstall": "manypkg check",
    "prepare": "husky install",
    "test": "pnpm test:unit && pnpm test:e2e",
    "test:unit": "turbo test:unit",
    "test:e2e": "turbo test:e2e",
    "test:e2e:playwright": "turbo test:e2e:playwright --force",
    "test:e2e:playwright:docker": "./scripts/run-e2e.sh",
    "test:watch": "turbo test:watch",
    "coverage": "pnpm turbo:coverage && pnpm report:merge && pnpm junit:merge",
    "turbo:coverage": "turbo coverage --force",
    "junit:merge": "jrm ./coverage/junit.xml ./packages/**/junit.xml",
    "report:merge": "monomerge && nyc report",
    "lint": "turbo build:config lint",
    "lint:fix": "turbo build:config lint:fix",
    "build": "turbo build --force",
    "build:config": "turbo build:config",
    "change": "changeset",
    "change:empty": "changeset --empty",
    "release": "./scripts/release.sh",
    "release:status": "changeset status",
    "release:preview": "./scripts/release-preview.sh",
    "apps:notification-center": "pnpm --filter notification-center dev",
    "clean": "pnpm /^clean:.*/",
    "clean:lock-file": "rm -f pnpm-lock.yaml",
    "clean:node_modules": "pnpm -r exec rm -rf node_modules && rm -rf node_modules",
    "clean:dist": "pnpm -r exec rm -rf dist && rm -rf dist"
  },
  "manypkg": {
    "ignoredRules": [
      "INTERNAL_MISMATCH"
    ]
  },
  "lint-staged": {
    "*.{js,ts,tsx}": "eslint --fix --ignore-path .gitignore",
    "*.{json,yaml,yml,md,mdx}": "prettier --write"
  },
  "dependencies": {
    "@changesets/cli": "^2.26.0",
    "@manypkg/cli": "latest",
    "codename": "^0.0.6",
    "cross-env": "^7.0.3",
    "eslint-config-custom": "workspace:*",
    "husky": "^8.0.3",
    "jest-junit": "^16.0.0",
    "junit-report-merger": "^4.0.1",
    "lint-staged": "^13.2.2",
    "monomerge": "latest",
    "nyc": "^15.1.0",
    "prettier-config": "workspace:*",
    "turbo": "latest"
  }
}
